# Original native roadside-oak trunk taper

The active roadside-oak base is derived from the retained parent under `assets/source/canopy-base/parents/roadside-oak-before-trunk-taper.{blend,glb,json}`. The other oak base is unchanged. This folder retains the exact executed native authoring script, raw native GLB, native measurements, packing transport source and verification. Runtime outputs are not authoring inputs.

Native Blender 5.2.1 LTS applies a 55% horizontal radial scale about the original bent shaft centerline through local height 1.65m, smoothly returning to 100% at 3.55m, below the first scaffold junction. The same continuous deformation moves the six intersecting root flares. All heights and upper wood positions remain exact. It changes 96 native wood vertices, with no new topology, objects, materials or maps. Both foliage meshes and all their native fields remain exact.

Actual triangle checks retain all six root contacts (14 intersections each) and the three lowest scaffold contacts (24,24,16 intersections), matching the parent. Every face remains nondegenerate with positive orientation; minimum face-normal alignment is 0.9597. Low wood bounds become x[-0.217737,0.221014], y[-0.193967,0.191307]m, maximum radial extent0.221817m, inside the existing0.35m collider half-width. The original ground penetration and upper attachment heights are unchanged.

The Blender exporter reorders some wood vertices. A verified bijection through the exact triangle-corner sequence transports only native wood POSITION/NORMAL fields into the original GLB packing. It changes256 packed position entries and272 normals (4,243 binary bytes). Original JSON and every other BIN byte remain exact, including both leaf POSITION/NORMAL/UV/COLOR/index streams and wood indices/UV/color. The result has8,536 triangles,651,132 raw bytes and383,739 gzip bytes before its accessibility field is added.

The active base GLB SHA256 is `c263048dc87e8f2580719d5631928255aa5768621bbe58b7a0f33541b31fd4d5`; editable no-field BLEND is `d9ad319d747e3c859a33e6a2e60d7ab08969a7d09e8b7cc5de21baf7d17c6511`. Raw native export SHA256 is `123fcabc8809524e4615d658e9c3fb83d71b23d9171d34a2b0fb8927efb10e47`. `base.json` declares all identities and the retained parent chain.

The wood changes physical occlusion, so the supported model build must bake a fresh roadside accessibility field. No old field is reused. The root task performs the combined supported build and final street/nearby-camera visual acceptance; these source checks do not claim that final visual test has passed.

For historical authoring replay, reconstruct an isolated project root with the retained parent files at `assets/source/canopy-base/roadside-oak.{blend,glb}` and the parent model record under the `models.roadside-oak` entry of its `base.json`. Pass that isolated root to `author-taper.py --project-root ... --output-root ...` after Blender's `--` separator. Do not pass the active already-tapered base as its parent. `native-authoring.json` retains the actual original execution input identities. `packing-transport.mjs` is the exact staging transport source, whose paths refer to the original staging workspace; the complete native field and original-packing evidence is retained here rather than requiring that scratch layout for normal production builds.
